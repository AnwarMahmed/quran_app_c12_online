import 'package:flutter/material.dart';

class SabhTabs extends StatelessWidget {
  const SabhTabs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
